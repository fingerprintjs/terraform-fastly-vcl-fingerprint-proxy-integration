{
  "name": "terraform-fastly-vcl-fingerprint-proxy-integration",
  "version": "2.0.0-rc.0",
  "private": true,
  "scripts": {
    "test": "echo \"there are no tests planned\"",
    "test:dts": "echo \"there are no typechecks planned\"",
    "build": "bash ./scripts/preparePackage.sh",
    "changeset:publish": "changeset publish",
    "changeset:version": "changeset version"
  },
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@fingerprintjs/changesets-changelog-format": "^0.2.0"
  }
}
